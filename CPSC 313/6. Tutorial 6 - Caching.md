## Computing Hits and Miss Rates
![[Pasted image 20251023123410.png]]
1. i
compulsory miss on first access
there are 32 elements so 32 reads, and after the first read everything is cached
31/32 hit rate

ii.
16 reads
15/16

iii
8 reads
7/8 hit rate

2. from (1) find the general formula being accessed and cache line size

$$
\begin{align}
\text{Reads}=\frac{\text{cache line}}{\text{data size}} \\
\text{Hit Rate} = \frac{\text{Reads} - 1}{\text{Reads}}
\end{align}
$$
3. 64 bytes one at a time at a time?

62/64 (1 compulsory miss at the start, second compulsory miss due to being unaligned by 8 bytes, need 1 more read to bring last 8 bytes in)

![[Pasted image 20251023125125.png]]

1.
i = 1 * 0.5 + (5 * 0.5)
= 3

ii. 2.6

iii = $\approx 15.4\%$

2.
25% hits 75% miss

hit time by 100 or miss time by 2

amdahl's low

$=\frac{1}{(1 - 0.25) + \frac{0.25}{100}}$ = 1.3289
or
$= \frac{1}{(1 - 0.75) + \frac{0.75}{2}}$ = 1.6

should invest in improving miss time by factor of 2

![[Pasted image 20251023130216.png]]

1. why cache into lines?

hardware limitations, we take advantage of the natural read size 

2. why not fully associative

linear scanning or prohibitive hardware costs
- awful for misses
- for hits and misses its better to be direct mapped

3. advantage of multi-way set associative cache 

if two cache lines will map to the same data????????? then the conflicts will become prohibitively expensive
- wait i dont understand how associativeness fixes this...
- or maybe it's the fact that we'll have fewer slots (index wise) but same amount of data
- i assume it's like folding down



summary
- balance between fast lookup blabla

read or write into three different arrays -> CEILING UP to four way set associative cache
- we just waste one bit otherwise highkey what is the point are u stupid

when is direct mapped better than 2 way no policy? random?
- if u iterate over arrays that are cache line size
- with direct mapped you get predictable # of hits and misses, highest possible
- with two way, you might not evict LRU