![[Pasted image 20251116181422.png]]
- where we're at now!

## Outline
- disk layout: what goes where, how large
- file system metadata: how do i find all the parameters of this file system
- free space management: how do i find the next block to allocate?
	- how do i deallocate a block?
- logical physical mapping: how do i find a particular block within a file
- directory structures: how do i make paths/directories work??

and we're going to explore tradeoffs


## How do we decide?
**fast!** we want to be fast working with large files, small files, and new blocks
**compact** - we have limited space, we want to make sure it's compact. what are we making compact?
- internal fragmentation? external?

**robustness**?
- how long lived
**usable**
- we want to make the file system usable for programmers and for us (we dont want our apis to be aheadache to use!)

## The Unix V6 File System
the first widely used file system for unix linux
released as full source code

- relatively simple ancestor of most modern file systems! (the fuck)
- disks were small! far smaller and far slower
	- compactness becomes the key goal

## Disk Layout
![[Pasted image 20251116181708.png]]
- pull yourself up by your bootstraps (you start at the boot block)
- information to help your computer get started
- we need to know where the superblock is always
- inode blocks, and then our data blocks

each block is 512 bytes

- struct filsys
	- today we call this the superblock
	- created when you create the file system
	- read when you mount the file system

![[Pasted image 20251116181801.png]]
- importantly **tells us the size of blcoks in the inode list!**
- how many blocks go into our inode list (can fit multiple inodes into one block)
- first fsize is the total volume, all blocks
![[Pasted image 20251116181916.png]]

each inode is just a C struct!
![[Pasted image 20251116181946.png]]
old int holy shid call them shorts
- what is i_addr (pbn, sixteen bytes for address)
- 4 for access time and 4 for modified time
- 32 bytes! per inode

## Big Lie #1 linkedlist of free blocks
- super block has a pointer to the first free block
- tiny disk, dont waste space tracking free blocks, use free blocks to track the free blocks!
- i see.. this is actually malloc
- would rather touch only the super block (probably cached)
- every allocate or deallocate messes with a random block
- just have 3 pointers, becomes a tree

![[Pasted image 20251116182242.png]]
block 11 is also free, if i use it i need to update my data structure
![[Pasted image 20251116182336.png]]
updates superblock to say there's only one free block
- 10 is our next node in our linkedlist, can't just lose all the data when someone writes everything inside of it!
- ![[Pasted image 20251116182423.png]]

- we can use block 10 to store data, but block 10 has pointers to other free data, and as such we'll copy that into the super block

**Deallocation?**
- need a place to have super block point to that the block is free!
- copy our super block into the newly freed block, and now our super block has a pointer to block 15!

## **Strengths** and weaknesses
- very little extra space!
  works a lot like a linked list style malloc/memory manager. We won't get a lot of contiguous or nearby blocks in a given file (not fantastic for caching)

![[Pasted image 20251116182627.png]]
a small lie cus we fucking store 100 .
you fucker

![[Pasted image 20251116182712.png]]
- for 8 or fewer blocks, simply contained directly in daddr (direct addresses) of data blocks
- max size of 4 Kb stored beacuse each block is 512 bytes
- pretty chill!
- indirect nodes! uhh i think he's talking about mixed? or not really its not our mixed layer
- ![[Pasted image 20251116182758.png]]
- this is helpful!

why does it ignore the last address?
last pointer is double indirect ok it is layered im capping
![[Pasted image 20251116182903.png]]

## is this usable?
- for small files, yeah pretty fast! no wasted indirect blocks to pass through
- is it compact?
- pretty small! not wasted indirect
- not that much internal fragmentation due to the small block size



![[Pasted image 20251116183054.png]]
- vnode is what we keep in memory to represent th inode


## How do we read the real file system?
- directory structures
- we already know how to build components and move from the root to the path

![[Pasted image 20251116183211.png]]
- sucked because of tiny file names but yea..... it was simple to implement

## Linux Second Extended File System: Ext2
- the second version!
- theres four but we aren' doing all that bull shit
- file system metadata: how do i find all the parameters of this file system
- disk layout
- free space manamgement
- logical to physical mapping
- directory structures (how do paths/directories work - bro i lowkey forgot already.. a crime)

![[Pasted image 20251117110819.png]]

![[Pasted image 20251117110827.png]]
- copies of super block and block descriptor table??
- where is the duplicated block stored?
- always at 1024
![[Pasted image 20251117110923.png]]
![[Pasted image 20251117110930.png]]
**why block groups?**
- a bit more organized than v6
- when feasible, allocate blocks in a file/files ina directory in as single block group that is physically nearby
- good group size will be stated in a few size

why do we have a superblock and a block descriptor?
- redundancy vs cost
	- we need some redundancy but not necessary to duplicate data on every block

## Free space management
- how do i deallocate the block

> [!NOTE] the bitmap
    > every bit represents one block, and if one bit is set then its used, if its 0 it is free
    > ![[Pasted image 20251117111231.png]]
    > very simple to understand!
    > significantly reduces number of disk accesses compared to v6 file system (we don't need to do an iterative traversal of the blocks)
    
- given the bitmap, could you quickly find a free block?
- find a sequence of consecutive free blocks?

- sounds like a solvable 221/320 problem
- if you do this in 436A you probably will be implementing one of these
- group size selected such that all blocks in a group are on the same cylinder
- no seeks to move between them

## Disk Use Example
- say we have 512 byte blocks, same from v6
- how many bits can one block hold
- $512 * 8 = 4096$
- so if a block is 512 bytes, a block can contain up to 4096 bits
- block of bitmap fo rinodes means up to 4096 inodes in a block group


*lets say we have 64 byte inodes how many blocs can we have in a group*
- how many inodes per block (8)
- how many blocks are we going to need (4096 / 8 inodes per block) = 512 blocks!

- continuing our unreasonable block size
- how many blocks of data on the disk can fit in this block group
  4096 bits in our one block of data block bitmap
- how many data blocks per block? well 1. it's a data block
- so 4096 blocks of data
	- is that enough for large files (not that it fits in one but is it practical)

caveat: block groups can be smaller than the maximum possible, but are all the same size? which is stored in the block descriptor table?

## Block Group Sizes explorations
**8 KB block size**
inodes are 128 bytes

how many bits in a block? 64 K bits
- each of our indoe and data bitmaps can allocate 64 k items
how much data can we have in a block group
- can have up to 64k blocks, each is 8 KB in terms of data, so 512 MB
maximum number of inodes we can have?
- 64K bits and each one corresopnds to a single inode
how many blocks will it take to hold these inodes?
- 8192 / 128 = 64 inodes per block, so 64k / 64 = 1024 blocks

![[Pasted image 20251117112121.png]]
## per file metadata
- 15 blocks
- 12 data blocks (direct)
- 1 indirect
- 1 double indirect
- 1 triple indirect

![[Pasted image 20251117112256.png]]
- maximum file size?
- whats the size of a PBN
- let's say 4 KB

number of PBNs for block 1024
direct ref 4kb
indirect ref = 1024 4 MB
double indirect = 1024 * 4 MB
triple = 1024 * 4 GB = 4 TB

- would stand the test of time generally

## Directory Structures
- nothign very surprising hierarchical directory structure
a directory contains a collection of directory enty structures
directories are 4 byte alligned
- no guarantee of a null byte at the end of the name! (as someone discussed on piazza)
```C
struct ext2_dir_entry {
	__le32 inode;
	__le16 rec_len;
	__le16 name_len;
	char name[256];
}
```

- rely on reclen and dont rely on anything else! we dont know if a string is null terminated
![[Pasted image 20251117112638.png]]

- the last directory entry is going to be the entirety of whatever is left in this block
- as we add new entries we resize it
![[Pasted image 20251117112720.png]]

### What happened later...
- ext3 in 2001
- added journaling, which helps a file system recovery and reduces chances of data loss
- journaling is similar to the logs kept by database systems
- max sizes: 16 GB to 2TB per file, 4TB to 32TB for the filesystem

- ext4
	- added
	- extents (instead of list of block addresses, we can have a list of arrays)
	- transparent encryption
	- improvement to how blocks are allocated
	- h-tree indices for very large directories
	- and more....

