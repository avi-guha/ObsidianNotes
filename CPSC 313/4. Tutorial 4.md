1.
```
A. irmovq 0x123, %rax
B. irmovq 0x456, %rbx
C. addq %rax, %rbx     # requires forwarding, 3 bubbles
D. irmovq 0x789, %rsi
E. irmovq 0xABC, %rdi
F. addq %rsi, %rdi     # requires forwarding, 3 bubbles
G. modq %rsi, %rax
H. divq %rbx, %rdi     # requires forwarding, 2 bubbles
```

part C reordering
```
A. irmovq 0x123, %rax
B. irmovq 0x456, %rbx
D. irmovq 0x789, %rsi
E. irmovq 0xABC, %rdi
C. addq %rax, %rbx     # requires forwarding, 1 bubble
F. addq %rsi, %rdi     # requires forwarding, considered above + 1
G. modq %rsi, %rax
H. divq %rbx, %rdi     # requires forwarding, 2 bubbles
```

4 total?


```
sum:                      # long sum(long *a, long n)
  andq %rsi, %rsi         
  je base                 # goto base if n == 0  # 2 squashed in the last iteration

  mrmovq (%rdi), %r8
  pushq %r8               # save *a on stack     # 1 stall

  irmovq $8, %r8
  addq %r8, %rdi
  irmovq $1, %r8
  subq %r8, %rsi
  call sum                # rax = sum(a+1, n-1)

  popq %r8                # 1 stall
  addq %r8, %rax          # rax = *a + sum(a+1, n-1)
  ret                     # 2 stalls

base:
  irmovq $0, %rax         # rax = 0
  ret
```

trying to improve? yo idk


ret always causes 3 stalls???? crazy.
oh wait actually it does make sense -> it literally needs to be put into getting nextPC so we can fetch the correct instruction, can't forward


```
long sum(long *a, long n) {
  int s = 0;
  for (long *p = a; p < a + n; p++)
    s += *p;
  return s;
}
```

```
sum:
irmovq $0, %rax
irmovq $8, %r8

loop:

mrmovq 0(%rdi), %rdx
addq %rdx, %rax



done:
```