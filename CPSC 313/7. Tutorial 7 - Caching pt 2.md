![[Pasted image 20251030123507.png]]

1. 
	a. both
	b. cache only

2.
- a) to memory technically?? but then we read it in (so the effect is of writing both)
- b) lalallaa only written to the cache, but the line is brought in 


sequential writes, one small part of memory
- definitely writeback
- also write-allocate (functionally write-through for misses)

cache coherence? 
all of the above
- even if dirty write states aren't handled
- we need to be invalidate cache lines, or bring the cache line from the data source to update it
- simplified

**strided access**
- accessing every x elements

![[Pasted image 20251030125119.png]]
a) 4 items per cache line
b) 1/2
c) 0

d) capacity of cache line?

2. 2048, cacheline 128 bytes

a) 32 items per cache line stride 8
3/4

b) 64 items per cache line
3/4

4 accesses

3/4

$$
\begin{align}
capacity = \frac{\text{cache line size}}{item\ size} \\
hit = \frac{\frac{\text{cap}}{stride} -1}{\frac{cap}{stride}}
\end{align}
$$

careful of alignment!

![[Pasted image 20251030131307.png]]

00 is ahh miss
01 is ahh hit
02 hit
03 hit
11 hit
12 hit
13 hit
22 miss
23 hit
33 hit

22 is the next miss.... ahh kill myeslf




misses 32
hits whatever - 32


hits will be lower
- misses will be the same