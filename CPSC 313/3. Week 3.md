# Prereadings
## Fetch stage
- what must we do in this stage
- read instruction memory
	- how large is the instruction?
	- break the instruction apart into various pieces
- what parts ofthe processor are invovled
	- PC
	- memory
	- PC-increment
	- logic to identify instruction

![[Pasted image 20250914223915.png]]
we're going to load into an internal register (pale blue ovals)
![[Pasted image 20250914224012.png]]

opcode is crucial for determining what instruction we're executing
- when we read from memory, we're going to populate these registers
- based on the opcode, that'll tell us how many bytes we need to read
- icode, ifun, ra, rb, valc
![[Pasted image 20250914224130.png]]
- figure out how to increment the PC
- produce potential next program counter value (valP)
	- potential because instruction may change it
## Decode
- determine what to read from the register file
- read those values

what parts are involved:
- rA, rB
- register file
- logic to determine which registers are used to produce valA and valB
![[Pasted image 20250914224340.png]]
- read those registers, produce values called valA and valB

nop,halt nah
move ! yes
alu instructions yep
jump ... hmm 
push/pop
call/ret

what opCode are we using? may imply we need rsp instead of rA or rB
- and icode

## Execute
- use the ALU
- set conditiosn codes

VERY COMPLICATED

uses
- valA, valB from register file
- valC from instruction
- use ALU
- set condition codes
- logic to select what inputs go in, and b) how to set condition codes

we have two alu inputs, alu a and alu b

![[Pasted image 20250914230005.png]]

somehow outputs will set the condition codes
- valC will be needed for address calculation!
- e.g. addition of constant in instruction to content of a specific register!
- opcode will help us choose whether we want to use valA or valC
- ALU also needs ifun, to know which function to apply

function will always be zero for these move functions, which means that we can put valC as zero

## Implementing Execute

push and pop, call and ret need to use the ALU
- bitter surprise
- we need the ALU to add 0 for a couple of other instructions
- rrmovq and irmovq?
	- we don't need anything here?
	- what we're going to find is that through consistency
	- we can reduce wiring and allow valE to get written directly to register file

e.g. rrmovq %rax, %rbx

valA goes into aluA
0 goes into aluB (maybe +/- 8)

we also need the opcode, to tell us if we want to update condition codes or not
- need a logic block *setcc*
- which tells us if we need to set cc

what do we do with those condition codes?
depending on output of condition codes, we'll feed it into a logic call *cond* (conditional)

given the function and particular state of the flags (Z,S) given the state of the flags and the function that we want, do we want to do the operation

irmovq also uses alu...

## Memory
- read or write to memory!
	- memory
	- valE (address)
	- valA (data)
	- valP (from PC increment)

**1. write to memory**
- what are we writing?
- valA! -> send it into data
- can't send it into data since we only use that for address calculation
- and only is used once per instruction
![[Pasted image 20250914232706.png]]

call output valM (the result of a read) -> has to get written back into register file

how do we know which register to write it to?
well, we'll have dstM which takes icode as a source and rA
memcntrl  will accept our icode to tell us if we need to produce valM

*how does pop/push work*?
need to go through alu to get valE to perform address calculation
for pop we need the address before we go through the alu!
- we will steal the address out of valB for pop operations
- to decide which of these two addresses to use, we need to get information from the opcode

ret/pop
- data values are different
- when we call, we are pushing the value of the address of next instruction
- we get that value from pc!
- take it from valP

![[Pasted image 20250914233239.png]]

## Writeback
- how do we detremine what register do we write back from!
- typically its just rB
- if we want to write into rB we'll tell dstE that's where we write things
- now we need to deal with conditional moves
	- take the condition code, and wire that into dstE
	- normally say, take rB and whatever just push it in if u need to
	- sometimes it'll say rather than take rb, use the stack pointer and update that

## PC update
- identify the address of the next instruction to execute
- PC
	- valP normal case
	- valM ret
	- valC call, jmp
	- cond

![[Pasted image 20250914234852.png]]
new component nextPC, we'll take valM as an input, valP, and valC
- wrap cond and put it in, determines if things are taken in conditional jumps
- addresses are 8 byte quadwords, valC may contain address of an address of an instruction


"when there's a quiz except for 0-5 we cancel a class so we don't add to the burden"

# Lecture
## Understanding execution
- first we understood each instruction in logical form - what it did
- then we saw how to realize instructions in hardware, using wiring diagrams
- today: execution as a set of formal specification statements
- wednesday: we will examine execution as a phased process routing signals

#### CALLING CONVENTIONS
- why?
	- covered in book and video
	- parameter passing
	- caller save registers (%rcx)
		- functions are allowed to modify them if they like
		- IF
			- function f calls function g and
			- f wants to use the value in %rcx after g returns
		- THEN
			- f needs to save %rcx before calling g
			- and restore it afterwards
		- because g might have modified %rcx
		- rdi, rsi, rdx, rcx, r8, r9, 10, r11 and return rax
	- callee save registers (%rbp)
		- functions can rely on the value of the register not changing
		- %rbx, %rbp, %r12, %r13, %r14
		- if 
			- function g wants to use `%rbp`
		- then
			- `g` needs to save %rbp before modifying it
			- `g` needs to restore the old value of `%rbp` before returning
		- because the function that called g expected `%rbp` to not change

things are pushed onto the stack in reverse order
(makes sense because it reads left to right)

we only have 15 registers, so we split them up into
**caller-save**, **callee-save**
- called function can do anything with those values, caller must save since they may be destroyed
- callee-save


![[Pasted image 20250915111409.png]]

## RRMOVQ THREEEE WAYS
![[Pasted image 20250915112034.png]]
![[Pasted image 20250915112103.png]]

wow.... unpacking rA:rB = $M_{1}[PC+1]$
valA = R\[rA]

ALU operation... add 0 to A, we don't want extra wires so it just makes it easier...

memory: nothing

writeback

rrmovq

pc update: valP because no conditionals

ifun and icode is a 4 bit signal, we're going to split the 8 bits into these two pieces


NOW let's try
***cmovxx***
- literally the rrmovq that sometimes doesn't do anything
- we need to check if the iFun and corresponding condition codes MATCH!!!

WHILE in the execute phase (adding 0 to get the value in M1{rA})
lets check the cnd = cond(CC, ifun)
- get a one bit signal, which tells us that we either writeback or don't
- ![[Pasted image 20250915113621.png]]

![[Pasted image 20250915113431.png]]
![[Pasted image 20250915113639.png]]

- we'll be writing logic blocks that will compiled to y86
![[Pasted image 20250915113804.png]]

fetch: icode, ifun, rA, rB, valC, valP, PC, Memory
Decode: rA, rB, valA, valB
writeback: cnd, rA, rB, valE, valM

# y86 TO SILICON: Lecture 2
*the closest we'll ever be*

preclass material has a mistake!!!!!
icode also needs to go into nextpc block

**Signal names
- why signals need to be routed where they are**
- fucking anatomy of a cpu this is bio

![[Pasted image 20250917110852.png]]
- there's no way for valM to feedback into the ALU
- still impossible
![[Pasted image 20250917111034.png]]
- we need to use the ALU twice!
- reserve two ... clock cycles for one instruction
- not possible still

![[Pasted image 20250917111420.png]]
![[Pasted image 20250917111425.png]]
![[Pasted image 20250917111434.png]]

## Implementing ALU-A
- valA
- iCode
- valC
- +/- 8

![[Pasted image 20250917111934.png]]
RRMOVQ, need to read registers, OPQ same
valC is needed to read offsets, MRMOVQ, RMMOVQ AND IRMOVQ (add 0 to valC)
+8

![[Pasted image 20250917112327.png]]
*it is the routing of information that is the core of the CPU, but we don't encode into C since we write it into circuits and gates!*

![[Pasted image 20250917112544.png]]
ternaries! so awful
![[Pasted image 20250917112554.png]]
this is fucking awful