## On a busy system, it's possible that all of memory will contain valuable pages
when that happens, what do we do when we need yet another page?
we've got this! a page replacement policy
- just like we learned in caching

but why are they not practical?
- they depend on the fact that the entity managing it (OS) has visibility into the access pattern
- every access to the cache is examined by the cache
- hardware software partnership for virtual memory is handled by the memory
- it doesn't know what's been accessed recently!


**two bits of information per page**
- access bits per PTE ( called use bits)
- dirty bits per PTE

- ideally an algorithm will 
	- do some approximation of LRU or an approximation of belady's
	- take advantage of the information that the HW does provide
	- make sure theres a lot of clean pages available, so we don't get stuck always doing a write before a read

## Intuition
- lru is finding the page that hasn't been used for the longest time
- since we have a lot of main memory pages, maybe it's ok to pick a page that hasn't been used for a long time
- we can take advantage of the use bits to determine pages that have not been used in a long time
	- can look at the use bits to determine which pages have been used since i've cleared some thing
- we won't do it in bulk, we'll do it when necessary

## Introducing the clock!
![[Pasted image 20251130150544.png]]
- each time a page is accessed, it's use bit is 1
- when we need to evict, if it's use bit is 1 we clear it and move the hand
- and if it's 0 we'll evict it! (meaning this page hasn't been used since the last clock cycle!)

![[Pasted image 20251130150800.png]]
- 55 isn't here!
- page 1 is set so we'll just unset it
- page 2 is not ste with it's use bit, so we can evict it and bring in 55!
![[Pasted image 20251130150833.png]]

if a page is dirty, it needs to write the new page and then replace it

### Clocks and beyond

*the second arm scans for dirty pages and will write them, and so the other arm is more likely to be able to find clean pages to evict*

- why clock and not lru
	- vast majority of accesses are handled in hardware and are invisible to the OS
- additional policies
	- should your process be allowed to evict my pages? (one application is using too many pages!)
	- should i let a single process consume all memory?
	- what is a fair page allocation?

## Questions!
- must cr3 contain a physical or virtual address
- holds root address of the TLB
- must be physical! (it's being used to map the virtual to physical, but if you used virtual address, you have to map, another virtual address?)

How large is a single page table?
- the size of a 
- 9 bits to index into page table
???/ im lost again
- every entry has 8 bytes
- there are 512 entries
- so $2^9$ to entries that need to be indexed
each entry is 64 bits (8 bytes)

therefore a page table takes up 4 KB
- and conveniently a page table is also 4 Kb!

**Why do PTEs contain physical page numbers?**
- how the fuck would you access memory

**why doesn't PTE need an offset?**
- lower bits are unused as we either reference a page in memory or a page table in memory
- pages by definition are aligned on 4 KB boundaries
- therefore the 12 low order bits at the beginning of a page or a page table are always 0.

**For each level page table, how much memory is reachable**
512 * 4KB = 2 MB of memory
2^9 * 2^12 = 2^21
i think we said this
perhaps
L4 * number of entries
2^21 * 2^9 = 2^30 (1 GB)


each level multiplies number of available entries by $2^9$ 

L2 = 512 GB
so L1 is 256 TB!

- L1 should access the entire address space, that's 48 bits on the x86-64 and conveniently, 48 bits is 256 TB. Wow!

**The l1 CI + CO happened to fit in the VPO**
yes! we get the offset early, which allows us to find the cache line in our l1 cache first
- once we finish translating memory we can instantly check if we match our line or it exists in the set
- helps us parallelize some work

I NEED MORE BITSSSSS

Page tables are the data structure that maps from virtual addresses
x86 implements 4 level page tables to conserve memory

![[Pasted image 20251201112107.png]]

## Virtual memory System Faults
## Page Fault:
- the virtual page being accessed is a valid page, but it is not in memory
	- yes the process gets to keep running
	- we'll transfer control over to the OS
	- the OS will bring that page back into memory (maybe evicting something with a clock)
	- updates the PTE

- process tries to write to a page whose write bit is 0
	- does the process get to keep running
	- NEOOWW
	- but try catch hurr durr
	- maybe...
		- if this is a fault, the OS jumps in to determine the situation, based on the extra data that the OS maintains
		- if it's a page that contained unwriteable data, but did have readable perms
			- operating system terminates the process
			- can't write to code, can't write to a string
		- a page is shared!
			- the page is shared with another process with CoW (Copy-on-write)
			- "remember what fork does? it makes a complete copy of the entire address space! and we exec right after we make a fork"
			- so actually we make a copy of the mappings to the address space, and we make all pages read only
			- but the OS knows.... who isn't allowed and who is! how?
				- when the process tries to write and we know that this is copy on write, we know this is a copy so we make a copy, copy all the data onto a new page, update the TLB
				- whoever wrote creates the copy, and this is done lazily, once done both the write bits are set back to 1
			- extensible to executable files

### **segmentation violation/segfault**
- the virtual page being accessed is invalid (there is nothing allocated to that part of the address space)
- does the process get to keep running? No!
- why can't the os let the process recover
	- the proecss has requested a non-existent object
	- the os has no way to signal that the load instruction should return an error
	- or just make it the fuck up????
	- if it fails just give it garbage

- why do you get one of these when you dereference a null pointer?
	- am i stupid or do i not remember this
	- 0 very bottom of address space
	- if its valid? depends on the OS

40 years ago, 0 was perfectly wonderful and valid!
- start at 4k! instead of 0
- you have to take away the whole page! the granularity is the page
- can't have a finer grained mapping

![[Pasted image 20251201113731.png]]

## Counting page faults
- remember questions from caching

"4kb cache 64 byte cache lines, given array of x, if you access every 4th byte just once whats your hit rate"
![[Pasted image 20251201114004.png]]
the tlb is incredibly fast. sub cycle


# Virtual Memory - the big picture
process isolation! one process's address space has no effect on another's unless those two processes explicitly set up communication

how does the vm work?
- hardware software partnership
- hardware translates whatever it can (TLB and page table) some only have a TLB
- software takes over wherever hardware leaves (page faults)

benefits?
- process address space can be larger than physical memory
- corollary: we can run a process even if some of its pages are not in memory
- we can run a collection of processes even if the sum of the sizes of their in use address spaces do not fit in memory
- we call loading of VM pages as they are needed = demand paging (it's kinda... lazy?)
- main memory (DRAM or RAM acts like a cache of rsum total of all processes address space
	- if memory is a cache, we need replacement policies
- the clock LALALALAL
	- an "approximation" of LRU
	- ok maybe not, more so like we know it just hasn't been used in a while

![[Pasted image 20251203111700.png]]

![[Pasted image 20251203111756.png]]


### Keep track of modified pages?
dirty bit! keep it in each PTE that is set on write
if page 7 in last example was dirty?
- have to writeback
- pretty fucking slow to writeback actually
	- need to write, need to read
	- likely to not be on the same cylinder, require a seek

- have a clean sweeping arm!!!
	- ensure that the pages we replace are clean

recall!
1, 3, 6, 55 4 5, SEVENTY SEVEN! we need to clear something! we get all the way to 7 and we clear it so we can write

**Two hands!**
- not the only way but a reasonable way
- our cleaner process goes through and writes back and dirty PTEs
- our other hand for replacement will keep looking for a clean page with use bit = 0
- writeback hand keeps a specific distance ahead of the replacement hand

> [!NOTE] Critical point
    > we can do this writing while the disk controller is no otherwise occupied, in parallel with work happening in the cores
    
![[Pasted image 20251203112740.png]]
x for x in 
bro i dont remember math anymore

## Policy decisions!
- how far ahead of the replacement hand should the writeback hand be
- how many pages should we write at a time?
- when should we move the write hand?

uh we do know. but we just do know that we want a pool of clean pages that the replacement hand can use when it has to

"what kind of contemporary replacement policies are there" (?)
- depends on hardware....
- we used to have not much memory... had to be aggressive in replacing
- now? we're lazy!

i was like this guy uses a data center as his personal laptop


## wrapping up!
- virtual memory is very cool because it connects a lot of stuff
- hardware/software interaction
- managing memory (everything is a cache)
- similarities between files and virtual memory
- management and exploitation of parallelism
- how tradeoffs at multiple levels impact user programs perofrmance

can learn about and take advantage of all of these!