## Hybrid Index Structure
Data Structure:
- the idnex is a fixed size array, small enough to fit in the inode
- most entries are direct pointers
- a few entries are single, double, or triple pointers

Given information:
- disk blocks 8192 bytes
- disk address 4 bytes
- 5 direct 1 indirect 1 double indirect, 1 triple indirect

Direct: 5 * 8192



How many bytes can be stored in the indirect pointers

- each block can contain

8192 / 4 = 2048 pointers
thus each indirect block can store 2048 * 8192 bytes

8192 / 4 pointers, thus can contain 2048 indirect blocks
double indirect

each double indirect pointer can contain (2048 ^ 2) * 8192 bytes

each triple INDIRECT pointer can contain
2048 double indirect pointers
SO
we can store (2048 ^ 3) * 8192 bytes

altogether now

Maximum file size
$5 * 8192 + 2048 * 8192 + (2048^2) * 8192 + (2048^3) * 8192 = 70380313886768$
in the petabyte range

## Multi-level index structure
- 8192 bytes
- 4 address
- 8 roots
- max height = 3
each level must have a homogenous depth
SO LETS GOOO

$8 \cdot 2048^3 \cdot8192$

2049 !! NYEAH
- multilevel = 3 blocks 
- hybrid = 2

example: 6
- can store up to 5 direct blocks in hybrid, but with multilevel we can store all 6 in the root
5
2048
1 + 1 1 (5)
- hybrid enters indirect level earlier than multilevel!

20

what is the smallest LBn for which the hybrid index can take fewer IOs tahn the multi-level index to fetch the associated block

what is the smallest LBN for multi-level taking fewer ios than the hybrid indxe to fetch

LBN 5 (6th block)
- 5 must be access via indirect in hybrid
- multilevel can represent as a direct level 

Q1
- what is the sequence of blocks accessed to /a/b/c

a - > 15

0s are invalid

## how many IO/s does it take for open vs read?
- read takes one more IO to actually read the file
- open simply finds the block num
- HOW MANY SYSTEM CALLS DOE SIT TAKE JUST KILL LLLLL ME

entry/dir/link/file

1 io for root
1 read entry for dir
1 io more
1 read dir for link
no io for .. cause have it in the cache
1 read inode block for alt to get file
^ very wrong... check photos

entry/dir/../alt/file

entry/alt/file